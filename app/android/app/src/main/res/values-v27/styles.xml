<?xml version="1.0" encoding="utf-8"?>
<!-- Display cutout, API 27 and up.

     shortEdges lets the window extend into the notch / punch-hole area. On a
     phone with a cutout the default (`default`) letterboxes the window below
     it in portrait, which is the same black band this whole change is about,
     just at the other end of the screen.

     Split into values-v27 rather than carrying tools:targetApi in the main
     file: the attribute does not exist before 27 and this is the one place
     the platform already knows that. -->
<resources>
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@color/skyBackground</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
